# Variáveis de ambiente para o perfil prod.
# Copie para .env e preencha — nunca commitar o .env real.

DB_URL=jdbc:postgresql://localhost:5432/maranata_music
DB_USERNAME=seu_usuario
DB_PASSWORD=sua_senha
